A hacky little class that will take a profile on Site A and find that person friends on Site B via social graph crawling, provided by Google's Social Graph API.

To enable more websites that we can find friends on, simply add the regex to the map in get_regex_for_profile. Like I sad, hacky.